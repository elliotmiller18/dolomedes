To join the network, 

I'm thinking we send a store at our node_id to the closest nodes that need it (using our genesis nodes)
That way we can both propagate ourself throughout the network during our travel to the nodes closest